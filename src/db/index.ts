import { drizzle } from "drizzle-orm/better-sqlite3"; import Database from "better-sqlite3"; import path from "path"; import * as schema from "./schema"; const dbPath = path.resolve(process.cwd(), "dev.db"); const sqlite = new Database(dbPath); sqlite.exec("CREATE TABLE IF NOT EXISTS products (id TEXT PRIMARY KEY, slug TEXT, name TEXT, category TEXT, price_cents INTEGER, compare_at_cents INTEGER, description TEXT, details TEXT, colors TEXT, sizes TEXT, images TEXT, featured INTEGER, is_new INTEGER, stock INTEGER, created_at TEXT); CREATE TABLE IF NOT EXISTS wishlists (id TEXT PRIMARY KEY, user_id TEXT, product_id TEXT, created_at TEXT);"); export const db = drizzle(sqlite, { schema });
